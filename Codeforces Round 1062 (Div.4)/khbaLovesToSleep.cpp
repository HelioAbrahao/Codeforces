#define ll long long 

#include<iostream>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr); 

    int number_of_test_cases; 
    cin >> number_of_test_cases; 

    ll number_of_friends, number_of_teleports, range_of_possible_positions;
    cin >> number_of_friends >> number_of_teleports >> range_of_possible_positions; 

    for(ll i = 0; i < number_of_friends; i++){
        
    }

    return 0; 
}