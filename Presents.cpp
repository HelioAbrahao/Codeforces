#include<iostream>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr); 

    int quantity_friends;
    cin >> quantity_friends;

    for(int i = 0; i < quantity_friends; i++){
        int number_of_friends_gave_gift_number_i;
    }

    // Cada um de seus amigos recebeu exatamente um presente. 

    return 0;
}